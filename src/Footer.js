import "./Design/Footer.css";

const Footer = () => {

    return (
        <footer className="footer">
            <p>Add social media links, donate links, and contact info</p>
        </footer>
    );
};

export default Footer;